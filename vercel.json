{
  "buildCommand": "npm install --legacy-peer-deps --no-audit --prefer-offline && npm run build",
  "outputDirectory": "dist",
  "framework": null,
  "installCommand": "echo 'Skipping default install'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
